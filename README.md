# Movie-Recommended-System
An end-to-end Content-Based Movie Recommender System built using Python. The engine implements NLP text preprocessing (PorterStemmer) and transforms raw metadata into dense 5,000-dimensional vector spaces via a Bag-of-Words paradigm. It utilizes Cosine Similarity to handle spatial direction math, serving live item pipelines via a Streamlit UI.
